#!/usr/bin/env zsh
DIR=${DOTDIR}/vim/bundle/pathogen
if [[ ! -d ${DIR} ]]; then
	git clone http://github.com/tpope/vim-pathogen ${DIR}
fi
ln -sf ${DIR}/autoload/pathogen.vim ${DOTDIR}/vim/autoload/pathogen.vim
