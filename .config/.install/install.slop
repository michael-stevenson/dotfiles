#!/usr/bin/env bash
set -e
hash cmake
hash git

pushd $(mktemp -d)
git clone https://github.com/naelstrof/slop.git
cd slop
cmake -DCMAKE_INSTALL_PREFIX=$HOME/opt -DCMAKE_INSTALL_RPATH=$HOME/opt/lib64 ./
make && make install
