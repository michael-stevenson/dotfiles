#!/usr/bin/env bash

d=$(mktemp -d)
cd $d

version="0.9.0"
wget https://github.com/ogham/exa/releases/download/v${version}/exa-linux-x86_64-${version}.zip
unzip exa-linux-x86_64-${version}.zip
mv exa-linux-x86_64 $HOME/opt/bin/exa
