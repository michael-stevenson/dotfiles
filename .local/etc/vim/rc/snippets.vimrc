" A bunch of standard snippets live here, the ones I use the
" most aren't quite to my tastes, so we will pick and choose
" rather than grab them all.  Still, an invaluable reference
" 'git://github.com/honza/vim-snippets'

let g:UlitSnipsExpandTrigger="<tab>"
let g:UltiSnipsJumpForwardTrigger="<tab>"
let g:UltiSnipsJumpBackwrdTrigger="<s-tab>"

let g:UltiSnipsSnippetDirectories=["ultisnips"]
