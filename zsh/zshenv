setopt null_glob

typeset -U fpath FPATH

default_funcs="$DOTDIR/zsh/functions"
fpath+=($default_funcs)

funcs=($default_funcs/*)
for f in ${funcs[@]}
do
	autoload ${f:t}
done

recursive_source ${DOTDIR} zshenv
recursive_source ${LOCALDOTDIR} zshenv
