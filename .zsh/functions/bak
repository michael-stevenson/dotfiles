if [[ -d $1 ]]; then
	new_name=$1.bak.`date +%Y%m%dT%H%M%S`
	tar -c -z -f ${new_name}.tar.gz $1
elif [[ -e $1 ]]; then
	new_name=$1.bak.`date +%Y%m%dT%H%M%S`
	cp $1 $new_name
	gzip $new_name
else
	echo "doesn't exist"
fi
